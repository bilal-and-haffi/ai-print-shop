import { postImageToPrintify } from "./route";

describe('postImageToPrintify', () => {
    it('should post the image to printify', () => {
        // Arrange
        // Assert
        postImageToPrintify
    }
    )
});